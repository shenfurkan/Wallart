<Window x:Class="WallArt.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="WallArt" Height="560" Width="540" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="CanResizeWithGrip" WindowStartupLocation="CenterScreen"
        Foreground="#EEEEEE" Icon="pack://application:,,,/Wallart.ico">
    <Window.Resources>
        <SolidColorBrush x:Key="BgBrush" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#252525"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#F0EAD6"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#FFFDF5"/>
        <SolidColorBrush x:Key="AccentPressedBrush" Color="#DED8C0"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3A3A3A"/>
        <SolidColorBrush x:Key="TextBrush" Color="#EFEFEF"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#A0A0A0"/>
        <SolidColorBrush x:Key="ButtonTextBrush" Color="#1A1A1A"/>
        
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource ButtonTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentPressedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="SecondaryButton" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
                 <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#222222"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="WindowButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="CloseButton" BasedOn="{StaticResource WindowButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A3A3A"/>
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                                <StackPanel Orientation="Horizontal" x:Name="HeaderPanel" IsItemsHost="True" Background="Transparent" Margin="0,0,0,-1"/>
                            </Border>
                            <ContentPresenter x:Name="PART_SelectedContentHost" Grid.Row="1" ContentSource="SelectedContent" Margin="0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="0,0,0,8"/>
            <Setter Property="Margin" Value="0,0,24,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontSize="14" FontFamily="Segoe UI" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource TextBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="{StaticResource BgBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="36"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Custom Title Bar -->
                <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Image Source="pack://application:,,,/Wallart.ico" Width="18" Height="18" Margin="14,0,8,0" VerticalAlignment="Center" IsHitTestVisible="False"/>
                    <TextBlock Grid.Column="1" Text="WallArt" VerticalAlignment="Center" FontFamily="Segoe UI" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource MutedTextBrush}" IsHitTestVisible="False"/>
                    
                    <Button Grid.Column="2" Style="{StaticResource WindowButton}" Content="—" FontSize="11" Click="MinimizeButton_Click"/>
                    <Button Grid.Column="3" Style="{StaticResource CloseButton}" Content="✕" FontSize="13" Click="CloseButton_Click" Margin="0,0,4,0"/>
                </Grid>

                <!-- Main Content -->
                <Grid Grid.Row="1" Margin="24,10,24,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header & Controls -->
                    <Border Grid.Row="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20">
                        <TabControl Background="Transparent" BorderThickness="0">
                            <!-- Dashboard Tab -->
                            <TabItem Header="Dashboard">
                                <StackPanel Margin="0,16,0,0">
                            <!-- Logo & Title -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Image Source="pack://application:,,,/Wallart.ico" Width="28" Height="28" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="WallArt" FontSize="20" FontWeight="Bold" FontFamily="Segoe UI" Foreground="{StaticResource TextBrush}"/>
                            </StackPanel>
                            <TextBlock Text="Curated public domain artworks fetched directly from premium museum collections." FontSize="13" FontFamily="Segoe UI" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Margin="0,0,0,24"/>
                            
                            <!-- Update Interval -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                                    <TextBlock Text="UPDATE INTERVAL" FontSize="11" FontWeight="Bold" FontFamily="Segoe UI" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal">
                                        <ComboBox ItemsSource="{Binding AvailableIntervals}" SelectedItem="{Binding UpdateInterval}" Width="70" Height="28" Padding="6,4" Background="#111" Foreground="#111" BorderBrush="{StaticResource BorderBrush}"/>
                                        <TextBlock Text="min" VerticalAlignment="Center" FontFamily="Segoe UI" Margin="8,0,0,0" Foreground="{StaticResource MutedTextBrush}"/>
                                    </StackPanel>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                                    <Button Command="{Binding TogglePauseCommand}" Margin="0,0,8,0">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                                                <Setter Property="Content" Value="Pause"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsPaused}" Value="True">
                                                        <Setter Property="Content" Value="Resume"/>
                                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    <Button Content="Clear Cache" Command="{Binding ClearCacheCommand}" Style="{StaticResource SecondaryButton}"/>
                                </StackPanel>
                            </Grid>
                            
                            <!-- Manual Update Row -->
                            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                <Button Content="Change Wallpaper Now" Command="{Binding ForceUpdateCommand}" Style="{StaticResource PrimaryButton}" ToolTip="Fetch a new artwork immediately" Margin="0,0,8,0"/>
                            </StackPanel>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,20,0,0" Visibility="{Binding IsUpdating, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ProgressBar IsIndeterminate="True" Width="120" Height="4" Foreground="{StaticResource AccentBrush}" Background="#111" BorderThickness="0" Margin="0,0,12,0"/>
                                <TextBlock Text="Fetching and processing artwork..." VerticalAlignment="Center" FontStyle="Italic" FontSize="13" FontFamily="Segoe UI" Foreground="{StaticResource MutedTextBrush}"/>
                            </StackPanel>
                                </StackPanel>
                            </TabItem>
                            
                            <!-- Museum Providers Tab -->
                            <TabItem Header="Museums">
                                <StackPanel Margin="0,16,0,0">
                                    <TextBlock Text="Select which museum collections to fetch artwork from:" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,16"/>
                                    
                                    <CheckBox Content="Art Institute of Chicago" IsChecked="{Binding UseArtInstituteOfChicago}" Foreground="{StaticResource TextBrush}" Margin="0,0,0,8"/>
                                    <CheckBox Content="Cleveland Museum of Art" IsChecked="{Binding UseClevelandMuseum}" Foreground="{StaticResource TextBrush}" Margin="0,0,0,8"/>
                                    <CheckBox Content="Metropolitan Museum of Art" IsChecked="{Binding UseMetropolitanMuseum}" Foreground="{StaticResource TextBrush}" Margin="0,0,0,8"/>
                                    <CheckBox Content="Victoria and Albert Museum" IsChecked="{Binding UseVictoriaAndAlbert}" Foreground="{StaticResource TextBrush}" Margin="0,0,0,8"/>
                                </StackPanel>
                            </TabItem>
                            

                            
                            <!-- History Tab -->
                            <TabItem Header="History">
                                <Grid Margin="0,16,0,0">
                                    <TextBlock Text="Recent Wallpapers. Currently not visually integrated." Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,16"/>
                                    <ListBox ItemsSource="{Binding History}" Background="Transparent" BorderThickness="0" Margin="0,24,0,0">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Margin="0,0,0,8">
                                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                                                    <TextBlock Text="{Binding Artist}" Foreground="{StaticResource MutedTextBrush}"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Grid>
                            </TabItem>
                            
                            <!-- Event Log Tab -->
                            <TabItem Header="Event Log">
                                <Grid Margin="0,16,0,0">
                                    <Border Background="#0C0A0A" BorderBrush="#332A2A" BorderThickness="1" CornerRadius="6" Padding="6">
                                        <ListBox ItemsSource="{Binding Logs}" Background="Transparent" Foreground="#D0D0D0" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" FontFamily="Consolas" FontSize="12">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="4,2"/>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                            <ListBox.ItemContainerStyle>
                                                <Style TargetType="ListBoxItem">
                                                    <Setter Property="Focusable" Value="False"/>
                                                    <Setter Property="Background" Value="Transparent"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="ListBoxItem">
                                                                <Border Background="{TemplateBinding Background}">
                                                                    <ContentPresenter/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ListBox.ItemContainerStyle>
                                        </ListBox>
                                    </Border>
                                </Grid>
                            </TabItem>
                        </TabControl>
                    </Border>
                </Grid>
            </Grid>
        </Border>

    </Grid>
</Window>
